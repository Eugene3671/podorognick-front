import React from "react";
import {
  QueryClient,
  HydrationBoundary,
  dehydrate,
} from "@tanstack/react-query";

const PoofileClien = async () => {
  return <div></div>;
};

export default PoofileClien;
